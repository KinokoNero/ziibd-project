package menu;

import models.Model;

public class AddOperation extends Operation {
    public AddOperation(String menuEntryText, String type, String model) {
        super(menuEntryText, type, model);
    }

    @Override
    public void addRecord(Model model) {

    }
}
